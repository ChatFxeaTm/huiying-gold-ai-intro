# DTGC v1.6 面板修复版安装说明

## 必需文件

把以下两个文件放在同一个 `MQL4/Experts` 目录：

1. `DTGC_v1_5_1_Chinese_MT4_NoWarnings.mq4`：基础交易核心。
2. `DTGC_v1_6_Chinese_PanelFix_MT4.mq4`：v1.6面板与已确认缺陷修复入口。

只编译并加载 `DTGC_v1_6_Chinese_PanelFix_MT4.mq4`，不要同时把两个EA加载到图表。

## 面板操作

- 展开状态：右上角 `－` 用于最小化。
- 最小化状态：只显示 `匯鹰.量化 +`，点击后恢复完整面板。
- 最小化状态会通过终端全局变量保存，重新加载后保持上次状态。

## 为什么采用两文件结构

v1.6复用已经通过 `0 errors, 0 warnings` 编译的v1.5.1交易核心，只替换面板、事件入口和少量已确认缺陷。这样可减少复制三千多行源码引入新错误的风险，也便于核对改动范围。

## 编译检查

当前交付需在用户的MetaEditor中正式编译。若出现错误或警告，请提供完整编译列表和行号，按实际编译反馈修正。